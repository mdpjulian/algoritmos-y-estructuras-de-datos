#include <iostream>
using namespace std;

int main()
{
	double s, rl, l, w, r2, t, r3;
	cout << "Ingrese longitud del lado del cuadrado:" << endl;
	cin >> s;
	rl = s * 4;
	cout << "Perimetro del cuadrado= " << rl << endl;

	cout << "Ingrese largo del rectangulo: " << endl;
	cin >> l;
	cout << "Ingrese ancho del rectangulo: " << endl;
	cin >> w;
	r2 = 2 * w + 2 * l;
	cout << "Perimetro del triangulo = " << r3 << endl;
return 0;
}